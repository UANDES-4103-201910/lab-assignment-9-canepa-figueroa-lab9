<p id="notice"><%= notice %></p>

<h1>Available Tickets</h1>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @tickets.each do |ticket| %>
    <tr>
      <td><%= link_to ("#{ticket.event.name}: $#{ticket.price}"), ticket %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<!--<%#= link_to 'Edit', edit_event_path(@event) %> |-->
<%= link_to 'Back', events_path %>
